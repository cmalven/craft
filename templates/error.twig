{# =============================================================== #}
{# General Error Template
{# =============================================================== #}

{% set entry = craft.entries.section('errorPages').slug('error').eagerly().one() %}

{% extends "pages/_entry" %}

{% do seomatic.meta.seoTitle(entry.title) %}
