{# =============================================================== #}
{# 503 Unavailable Error Template
{# =============================================================== #}

{% set entry = craft.entries.section('error503').eagerly().one() %}

{% extends "pages/_entry" %}

{% do seomatic.meta.seoTitle(entry.title) %}
