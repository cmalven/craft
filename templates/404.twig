{# =============================================================== #}
{# 404 Error Template
{# =============================================================== #}

{% set entry = craft.entries.section('error404').eagerly().one() %}

{% extends "pages/_entry" %}

{% do seomatic.meta.seoTitle(entry.title) %}
