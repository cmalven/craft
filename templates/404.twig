{# =============================================================== #}
{# 404 Error Template
{# =============================================================== #}

{% set entry = craft.entries.section('errorPages').slug('404').eagerly().one() %}

{% extends "pages/_entry" %}

{% do seomatic.meta.seoTitle(entry.title) %}
